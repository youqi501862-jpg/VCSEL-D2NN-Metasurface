# 最终提交前待办清单

更新日期：2026-08-09

> 当前成果边界：算法仿真 + COMSOL 单元仿真 + 结构映射 preview。以下待办以完成学校大创结题材料为目标，不默认要求真实加工和硬件实验。

## 1. 必须完成

- [ ] 整理最终报告：将阶段总报告内容合并到学校模板，统一图表编号、术语和参考路径。
- [ ] 检查 PPT：建立正式 `.pptx`，逐页核对 baseline、PTQ、QAT、robustness、LUT 和 mapping 数字。
- [ ] 保存 COMSOL 模型文件：归档 `.mph` 文件、软件版本、材料参数、边界条件、激励、网格和 sweep 设置。
- [ ] 保存所有正式 CSV 和图片：避免只提交 Markdown 结论而没有原始结果依据。
- [ ] 统一文件路径：明确根目录 `outputs` 与代码仓库内 `outputs` 的分工，建议在最终报告使用相对路径。
- [ ] 写清楚局限：明确合成数据、算法扰动、COMSOL 单元模型和 radius map preview 的边界。
- [ ] 对提交包做一次离线可读性检查：在不依赖训练环境的情况下能够打开报告、CSV、PNG 和 PPT。

## 2. 建议完成

- [ ] 完成 VCSEL 4-level phase-to-radius 映射，形成两条任务线可对照的结构 preview。
- [ ] 增加更多 COMSOL 参数组合，包括高度、周期、材料参数、入射波长和偏振条件。
- [ ] 开展 alignment-aware training，把层间错位扰动加入训练或数据增强。
- [ ] 优化更完整的 phase coverage，减少 LUT 相位缺口并同时约束透射率。
- [ ] 增加多随机种子实验，报告均值和标准差，降低单次训练偶然性。
- [ ] 补充 COMSOL meta-atom 场分布图，增强单元机理展示。

## 3. 可以不做

- [ ] 完整 `128×128` 阵列 COMSOL 全波仿真：计算资源消耗极大，不是当前阶段闭环的必要条件。
- [ ] 真实器件加工：可作为长期目标，不应作为当前已有成果进行表述。
- [ ] 全部 2/4/8/16 模型都做半径映射：阶段展示保留代表性的 4-level QAT dense mapping 即可。

## 4. 推荐提交前顺序

1. 先保存 COMSOL `.mph` 和仿真参数说明。
2. 再完成正式 PPT 与最终报告。
3. 将 CSV、PNG、NPY、checkpoint 和报告按文件清单归档。
4. 最后进行数字一致性检查和路径有效性检查。
