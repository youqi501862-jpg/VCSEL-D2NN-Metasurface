# GitHub Release Safety Check

检查日期：2026-08-31

## 发布范围

本发布目录由原项目的只读副本整理得到，没有修改或删除原项目文件，没有重新训练模型。仓库定位为算法仿真、COMSOL meta-atom 单元仿真和 phase-to-radius 结构映射 preview，不是真实硬件实验，也不是最终加工版设计。

## 安全检查结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| 大于 50 MB 的待提交文件 | 通过 | 无 |
| 密码、token、API key | 通过 | 未匹配到凭据模式 |
| 私人绝对路径和账户名 | 通过 | 发布文本中未保留本机项目路径或 Windows 用户目录 |
| checkpoint | 通过 | 无 `.pt/.pth/.ckpt/.onnx` |
| COMSOL 原始模型 | 通过 | 无 `.mph/.mphbin` |
| NumPy 大数组 | 通过 | 无 `.npy/.npz` |
| 虚拟环境和缓存 | 通过 | `.gitignore` 排除环境、缓存与测试生成物 |
| README 图片路径 | 通过 | 6 个引用均为相对路径且文件存在 |
| 重复/临时脚本 | 通过 | 未纳入 quick、smoke、debug、恢复脚本和实验性 Lumerical 文件 |

## 测试结果

- 用户指定的 5 个工程入口均通过 `py_compile`。
- 发布目录内共 21 个 Python 文件均通过 `py_compile`。
- `unittest` 共发现 8 项：7 项通过，1 项按设计跳过。
- 跳过项需要原始 `outputs/`，而公开仓库按安全策略不上传该目录；其他路径、LUT、映射和发布布局测试均通过。

## 未纳入仓库的内容

| 类别 | 原项目统计 | 排除原因 |
|---|---:|---|
| 生成数据集 | 约 129.50 MB | 可重新生成，避免仓库膨胀 |
| 根目录 `outputs/` | 约 16.90 MB | 包含大量中间结果、样本和模型派生文件 |
| checkpoint | 22 个，约 18.76 MB | 安全和仓库体积策略 |
| NPY/NPZ 数组 | 138 个，约 12.14 MB | 使用 CSV、PNG 和报告代替展示 |
| COMSOL `.mph/.mphbin` | 原项目未发现 | 原始模型未进入本次发布源 |
| pipeline / COMSOL field 图 | 原项目未发现 | 不生成未经验证的替代图 |

## 结论

发布候选内容规模约 2.21 MB（生成本检查文件前），适合普通 Git 提交。README、精选脚本、报告、图片和轻量 COMSOL 结果均已纳入；被排除内容由 `.gitignore` 继续保护。
